# 2M Confecções — Landing Page

Landing page de captura para WhatsApp (atacado).

## Local

```bash
python -m http.server 8765
```

Abra `http://127.0.0.1:8765/`

## Deploy (Vercel)

1. Importe o repositório `Leonardotrentini/2m` na Vercel
2. Framework Preset: **Other**
3. Build Command: (vazio)
4. Output Directory: `.` (raiz)
5. Deploy

Ou via CLI:

```bash
npx vercel --prod
```

## Tracking

- Meta Pixel: `1051143143964005`
- Eventos: `PageView` (load) + `Contact` (clique no CTA)
- Destino: WhatsApp `+55 47 99952-2331`
